# PushMaker - GitHub Pages Deploy Workflow
# This workflow builds and deploys the portfolio site

name: Deploy PushMaker Site

on:
  push:
    branches:
      - main
    paths:
      - 'index.html'
      - 'assets/**'
      - 'site-config.json'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Generate Site
        run: |
          # Create dist folder
          mkdir -p dist

          # 1. Copy Main Entry
          if [ -f "index.html" ]; then
            echo "📄 Copying index.html..."
            cp index.html dist/index.html
          else
            echo "⚠️ No index.html found!"
          fi

          # 2. Copy Data Snapshot
          if [ -f "github-data.json" ]; then
            echo "📊 Copying github-data.json..."
            cp github-data.json dist/github-data.json
          fi

          # 3. Copy Config
          if [ -f "site-config.json" ]; then
            cp site-config.json dist/site-config.json
          fi

          # 4. Copy Public Assets
          if [ -d "public" ]; then
            echo "📂 Copying public assets..."
            cp -r public/* dist/ 2>/dev/null || :
          fi
          
          echo "✅ Build preparation complete."

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
